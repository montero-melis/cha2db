y <- sw.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data (targets only)", )
text(x, y, labels = row.names(sw.tgt.docsim), cex=.7)
# Spanish
sp.tgt.mds <- cmdscale(1-sp.tgt.docsim, eig=TRUE, k=2)
x <- sp.tgt.mds$points[,1]
y <- sp.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Spanish data (targets only)", )
text(x, y, labels = row.names(sp.tgt.docsim), cex=.7)
# All
all.tgt.mds <- cmdscale(1-all.tgt.docsim, eig=TRUE, k=2)
x <- all.tgt.mds$points[,1]
y <- all.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for all data (targets only)", )
text(x, y, labels = row.names(all.tgt.docsim), cex=.7)
library('stats')
cmdscale(1-sw.tgt.docsim)
hclust(1-sw.tgt.docsim)
32*32
length(sw.tgt.docsim)
is.na(sw.tgt.docsim)
sum(is.na(sw.tgt.docsim))
ead(sw.tgt.docsim)
head(sw.tgt.docsim)
head(1-sw.tgt.docsim)
hclust(1-sw.tgt.docsim)
hclust(1-sw.tgt.docsim, method="complete", members=NULL)
hclust(1-sw.tgt.docsim, method="average", members=NULL)
hclust(sw.tgt.docsim, method="average", members=NULL)
str(sw.tgt.docsim)
str(as.matrix(sw.tgt.docsim))
attr(all.docsim, "Size")
?dist
dist(all.docsim)
dist(1-all.docsim)
sw.mds <- cmdscale(1-sw.docsim, eig=TRUE, k=2)
x <- sw.mds$points[,1]
y <- sw.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data", )
text(x, y, labels = row.names(sw.docsim), cex=.7)
d.sw <- dist(1-sw.docsim) # euclidean distances between the rows
fit <- cmdscale(d.sw,eig=TRUE, k=2) # k is the number of dim
fit # view results
# plot solution
x <- fit$points[,1]
y <- fit$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="Metric MDS", type="n")
text(x, y, labels = row.names(sw.docsim), cex=.7)
par(mfrow=c(1,2))
# WITH distractor items
# Swedish
sw.mds <- cmdscale(1-sw.docsim, eig=TRUE, k=2)
x <- sw.mds$points[,1]
y <- sw.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data", )
text(x, y, labels = row.names(sw.docsim), cex=.7)
d.sw <- dist(1-sw.docsim) # euclidean distances between the rows
fit <- cmdscale(d.sw,eig=TRUE, k=2) # k is the number of dim
fit # view results
# plot solution
x <- fit$points[,1]
y <- fit$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="Metric MDS", type="n")
text(x, y, labels = row.names(sw.docsim), cex=.7)
?dist
#Swedish data
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=5, border="red")
par(mfrow=c(1))
par(mfrow=c(1,1))
## Hierarchical clustering
#Swedish data
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=5, border="red")
## Hierarchical clustering
#Swedish data
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
fit <- hclust(d, method="average")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="average")
plot(fit) # display dendogram
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="single")
plot(fit) # display dendogram
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="complete")
plot(fit) # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=5, border="red")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=5, border="red")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=4) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=4, border="red")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=6, border="red")
## Hierarchical clustering
#Swedish data
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit, horiz=T) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=6, border="red")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(as.dendogram(fit), horiz=T) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=6, border="red")
?plot
require('graphics')
?require
library('graphics')
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(as.dendogram(fit), horiz=T) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
# draw dendogram with red borders around the 5 clusters
rect.hclust(fit, k=6, border="red")
d <- dist(1-sp.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(as.dendogram(fit), horiz=T) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red")
d <- dist(1-sw.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # d
d <- dist(1-sp.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red")
?plot
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit, main="Dendogram for Swedish data -- target items") # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
# Swedish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit, main="Dendogram for Swedish data: target items") # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit) # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red")
# Spanish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red")
# Swedish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=5) # cut tree into 5 clusters
rect.hclust(fit, k=5, border="red")
# WITHOUT distractor items
# Swedish
sw.tgt.mds <- cmdscale(1-sw.tgt.docsim, eig=TRUE, k=2)
x <- sw.tgt.mds$points[,1]
y <- sw.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data (targets only)", )
text(x, y, labels = row.names(sw.tgt.docsim), cex=.7)
# Spanish
sp.tgt.mds <- cmdscale(1-sp.tgt.docsim, eig=TRUE, k=2)
x <- sp.tgt.mds$points[,1]
y <- sp.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Spanish data (targets only)", )
text(x, y, labels = row.names(sp.tgt.docsim), cex=.7)
# All
all.tgt.mds <- cmdscale(1-all.tgt.docsim, eig=TRUE, k=2)
x <- all.tgt.mds$points[,1]
y <- all.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for all data (targets only)", )
text(x, y, labels = row.names(all.tgt.docsim), cex=.7)
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=6) # cut tree into 5 clusters
rect.hclust(fit, k=6, border="red")
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=3) # cut tree into 5 clusters
rect.hclust(fit, k=3, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=3) # cut tree into 5 clusters
rect.hclust(fit, k=3, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=4) # cut tree into 5 clusters
rect.hclust(fit, k=4, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=4) # cut tree into 5 clusters
rect.hclust(fit, k=4, border="red")
# Swedish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=2) # cut tree into 5 clusters
rect.hclust(fit, k=2, border="red")
# Swedish data
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=3) # cut tree into 5 clusters
rect.hclust(fit, k=3, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=3) # cut tree into 5 clusters
rect.hclust(fit, k=3, border="red")
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red")
par(mfrow=c(1,2))
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red")
par(mfrow=c(2,1))
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red")
par(mfrow=c(1,2))
d <- dist(1-sw.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Swedish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red") # draw dendogram with red borders around the 5 clusters
# Spanish data
d <- dist(1-sp.tgt.docsim) # euclidean distances matrix from dissimilarity matrix
fit <- hclust(d, method="ward")
plot(fit,
main="Dendogram for Spanish data: target items",
xlab="videoclips") # display dendogram
groups <- cutree(fit, k=7) # cut tree into 5 clusters
rect.hclust(fit, k=7, border="red")
# examples of related terms
associate(w.all.matrix, "sube")
associate(w.all.matrix, "upp")
associate(w.all.matrix, "drog")
nb.sube <- associate(w.all.matrix, "sube")
nb.upp <- associate(w.all.matrix, "upp")
nb.drog <- associate(w.all.matrix, "drog")
View(loc)
rm(loc)
nb.sube
as.data.frame(nb.sube)
xtabs(as.data.frame(nb.sube))
xtable(as.data.frame(nb.sube))
xtable(as.data.frame(nb.drog))
## MDS of distances between documents, based on Sw, Sp and all data
par(mfrow=c(1,1))
# WITH distractor items
# Swedish
sw.mds <- cmdscale(1-sw.docsim, eig=TRUE, k=2)
x <- sw.mds$points[,1]
y <- sw.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data", )
text(x, y, labels = row.names(sw.docsim), cex=.7)
# Spanish
sp.mds <- cmdscale(1-sp.docsim, eig=TRUE, k=2)
x <- sp.mds$points[,1]
y <- sp.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Spanish data", )
text(x, y, labels = row.names(sp.docsim), cex=.7)
# All
par(mfrow=c(1,2))
# WITH distractor items
# Swedish
sw.mds <- cmdscale(1-sw.docsim, eig=TRUE, k=2)
x <- sw.mds$points[,1]
y <- sw.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data", )
text(x, y, labels = row.names(sw.docsim), cex=.7)
# Spanish
sp.mds <- cmdscale(1-sp.docsim, eig=TRUE, k=2)
x <- sp.mds$points[,1]
y <- sp.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Spanish data", )
text(x, y, labels = row.names(sp.docsim), cex=.7)
# All
summary.textmatrix(all.matrix)
as.data.frame(summary.textmatrix(all.matrix))
xtable(summary.textmatrix(all.matrix))
table(summary.textmatrix(all.matrix))
as.vector(summary.textmatrix(all.matrix))
r(summary.textmatrix(all.matrix))
(summary.textmatrix(all.matrix))
all.matrix
all.matrix!=0
sum(all.matrix!=0)
sum(all.matrix==0)
# WITHOUT distractor items
# Swedish
sw.tgt.mds <- cmdscale(1-sw.tgt.docsim, eig=TRUE, k=2)
x <- sw.tgt.mds$points[,1]
y <- sw.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Swedish data (targets only)", )
text(x, y, labels = row.names(sw.tgt.docsim), cex=.7)
# Spanish
sp.tgt.mds <- cmdscale(1-sp.tgt.docsim, eig=TRUE, k=2)
x <- sp.tgt.mds$points[,1]
y <- sp.tgt.mds$points[,2]
plot(x, y, xlab="Coordinate 1", ylab="Coordinate 2",
main="MDS of videoclips for Spanish data (targets only)", )
text(x, y, labels = row.names(sp.tgt.docsim), cex=.7)
# All
